qbs_free(__STRING_OUTNAME);
qbs_free(__STRING_N);
qbs_free(__STRING_FNAME);
qbs_free(__STRING_RAWBIN);
qbs_free(__STRING_S);
if (__ARRAY_UBYTE_LASTOUTARRAY[2]&1){
if (__ARRAY_UBYTE_LASTOUTARRAY[2]&4){
cmem_dynamic_free((uint8*)(__ARRAY_UBYTE_LASTOUTARRAY[0]));
}else{
free((void*)(__ARRAY_UBYTE_LASTOUTARRAY[0]));
}
}
free_mem_lock( (mem_lock*)((ptrszint*)__ARRAY_UBYTE_LASTOUTARRAY)[8] );
qbs_free(__STRING_H);
qbs_free(__STRING_A);
